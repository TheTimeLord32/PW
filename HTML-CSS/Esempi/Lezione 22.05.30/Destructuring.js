let options = {
	title: "Menu",
	width: 100,
	height: 200
};

let { title, width: w, height: h } = options;

console.log(title);
console.log(h);
console.log(w);
// console.log(width); non viene più riconosciuta
